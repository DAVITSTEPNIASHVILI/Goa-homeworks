ფუნქცია არის ორ ნაირი: ერთი ისეთი რომელიც გამზადებულია და არ ვიცით რა ხდება შიგნით და მეორე ჩვენი შექმნილი ფუნქცია რომელშიც ვიცით რა ხდება. ფუნქციის გამოყენება ჯობია  რადგან იგი უფრო მოსახერხებელია.

def ნიშნავს define შექმნა, ჩამოყალიბებას. მას ვიყენებთ ჩვენი ფუნქციის შექმნის დროს.
function name არის ფუნქციის სახელი.
funqtion parameter არის ფუნქციის სახელში ჩაწერილი ნებისმიერი რამე რომელიც  უნდა გამოიყენო ფუნქციაში.
indentation არის 1 ტაბით მარჯვნივ გადაწევას და კოდის ლამაზად და სწორად წერა.
code block არის კოდის ბლოკი და ეს ნიშნავს იმ ფუნქციაში არსებული ინფორმაცია.
function call არის ფუნქციის გამოძახება.
function argument არის ფუნქციის არგუმენტი რომელიც იწერება ფუნქციის გამოძახებისას.
